# Prisma reads DATABASE_URL from this file for generate / migrate / studio.
# Use 127.0.0.1 (not localhost) to avoid IPv6 resolution issues on Windows.
DATABASE_URL="postgresql://chorify:chorify@127.0.0.1:5432/chorify?schema=public"
