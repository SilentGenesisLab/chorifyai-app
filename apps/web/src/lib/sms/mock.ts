/** Dev provider: print the login code to the server console. */
export async function sendViaMock(phone: string, code: string): Promise<void> {
  console.log(
    `\n┌─────────────────────────────────────────────┐\n` +
      `│  [SMS:mock] ${phone}  验证码 ${code}\n` +
      `│  (5 分钟内有效 · 设置 SMS_PROVIDER=aliyun 发真实短信)\n` +
      `└─────────────────────────────────────────────┘\n`,
  );
}
